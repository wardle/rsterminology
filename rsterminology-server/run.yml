jdbc:
  rsdb:
    url: jdbc:postgresql:rsdb
    driverClassName: org.postgresql.Driver
    initialSize: 10

cayenne:
  datasource: rsdb
  createSchema: false

log:
  level: warn
  loggers:
    org.apache.cayenne:
      level: warn
